describe('{{ folder }}', () => {
  it.each`
    input | expected
    ${}   | ${}
  `('should return $expected with input $input', ({ input, expected }) => {
    expect.hasAssertions();
    expect().toBe(expected);
  });
});
